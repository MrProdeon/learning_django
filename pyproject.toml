[tool.poetry]
name = "learning-django"
version = "0.1.0"
description = ""
authors = ["prodeon21@gmail.com"]
readme = "README.md"

packages = [
    { include = "src" }
]

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
